fix(backend): harden live ingestion endpoint (3 issues)

High — non-finite floats crash MySQL on save
  Add per-field validate_<field>() methods to LiveSampleSerializer that
  call math.isfinite() and raise ValidationError for NaN / Inf / -Inf,
  returning 400 before the value ever reaches the ORM.

Medium — minimal payload silently skipped due to offline validator
  Add validate_live_record() to validator.py — ancillary fields are
  optional; only fields that ARE present are range-checked. Swap
  validate_record (offline, requires all 7 fields) for
  validate_live_record in LiveIngestView. A payload with only
  run_id + timestamp + soil_moisture now yields preprocess_status="valid".
  Export validate_live_record from estimation.ingestion public API.

Medium — concurrent POSTs could produce duplicate cycle_index (500)
  Move last-cycle lookup, _restore_state, step, and PipelineCycle.save()
  inside transaction.atomic() with select_for_update() on the run row.
  This serialises concurrent requests for the same live run and prevents
  IntegrityError from the (run, cycle_index) unique constraint.

Tests: 28 → 40 (+12); full suite: 474 → 486 passed.
